"use strict";
(
    function (app) {
        app.controller('footer', [
            '$rootScope', '$scope',
            function (root, scope) {
                root.resource.$promise.then(function (data) {
                    
                });
            }
        ]);
    }
)(URC);